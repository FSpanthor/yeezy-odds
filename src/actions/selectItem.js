//SET_SIZE
export const setSize = (size) => ({
	type: "SET_SIZE",
	size,
});

//SET_ENTRIES
export const setEntries = (entries) => ({
	type: "SET_ENTRIES",
	entries,
});

//SET_DROP
export const setDrop = (drop) => ({
	type: "SET_DROP",
	drop,
});

//SIM_COMPLETE
export const setSimComplete = (complete) => ({
	type: "SET_SIM_COMPLETE",
	complete,
});
